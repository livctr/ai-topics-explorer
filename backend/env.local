# For local development only. `mv env.local .env` to use it.
# For production, use `.env` at the root level (done in Dockerfile).

POSTGRES_HOST=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
POSTGRES_PORT=
